# Achievements page override

- On mobile, the certificate carousel shows exactly one full-width card per viewport with no adjacent-card preview.
- Preserve mandatory horizontal snapping and the existing previous/next controls.
- The certificate track must clip vertical overflow and scroll only on the horizontal axis.
- At `sm`, show two cards; at `lg`, show three cards.
- Keep the established white, cyan, rounded-card visual language.
